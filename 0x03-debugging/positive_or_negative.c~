#include <stdio.h>

void positive_or_negative(int n)
{
  if(n>0)
    {
      printf("%d is negative",n);
    }
  if(n == 0)
    {
      printf("%d is zero",n);
    }
  if(n<0)
    {
      printf("%d is negative",n);
    }
}
