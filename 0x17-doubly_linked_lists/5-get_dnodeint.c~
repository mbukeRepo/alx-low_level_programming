#include "lists.h"

dlistint_t *get_dnodeint_at_index(dlistint_t *head, unsigned int index)
{
unsigned int i = 0;
dlistint_t *curr = head;
while (i < index)
{
curr = curr->next;
i++;
}
return (curr);
}
