#include <string.h>
/**
 * _strcpy - copies strings
 * @dest: destination
 * @src: source
 * Return: pointer to a string
 */
char *_strcpy(char *dest, char *src)
{
strcpy(dest, src);
return (dest);
}
